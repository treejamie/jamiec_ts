Ensure you've read AGENTS.md.

In this work, you will create a new hono project that has one home page route "/" 

This work is limited strictly to bun and hono.  I've already installed bun use mise, so don't attempt to re-installed it again.

Write a test to assert the route at / returns a 200 response.

