Ensure you've read AGENTS.md

In this next work you are to create a github action file that copies the testing in the source project.

When code is pushed to a new branch, the tests should run.  Wherever possible using cache efficiencies so the actions run as fast as they can.